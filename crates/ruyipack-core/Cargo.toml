[package]
name = "ruyipack-core"
version = "0.1.0"
edition = "2024"

[dependencies]
indexmap = { workspace = true, features = ["serde"] }
serde = { workspace = true, features = ["derive", "serde_derive"] }
